#require "../mahiru_decentral.eflint".

// initialisation based on site information
+local-site(X).
+data-asset(DX1).
+data-asset(DX2).
+domain(".co.uk").

// initialisation based on consortium info
+member(X).
+member(Y).
+member(Z).

// initialisation based on local donor information
+donor(Xander). give-consent(Xander,X,DIPGResearch).
+donor(Xandra). give-consent(Xandra,X,DIPGResearch).
+subject-of(donor,data).

// updates received from DIPG registry server, in this case relating to project P1
#require "P1.eflint".

// no access for any site (ownership not being considered in current example)
?!may-access.
