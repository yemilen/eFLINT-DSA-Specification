#require "../mahiru_union.eflint".

+local-site(Z).
+data(DZ1).
+data(DZ2).
+domain(".it").

+member(X).
+member(Y).
+member(Z).


+donor(Zara). // no consent given
+subject-of(donor,data).

#require "P1.eflint".

?!may-access.
