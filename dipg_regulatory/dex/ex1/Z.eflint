#require "../mahiru_decentral.eflint".

+local-site(Z).
+data-asset(DZ1).
+data-asset(DZ2).
+domain(".it").

+member(X).
+member(Y).
+member(Z).


+donor(Zara). // no consent given
+subject-of(donor,data).

#require "P1.eflint".

?!may-access.
